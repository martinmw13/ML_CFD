# CLAUDE.md

Guidance for agents working in this repository. `mlcfd` is a Python package for
dimensionality reduction of CFD snapshot data (PCA, manifold learning, Kernel PCA,
autoencoders). See `README.md` for install and usage.

## Agent skills

### Issue tracker

Issues and PRDs are tracked on GitHub (`martinmw13/ML_CFD`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles using default label names (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
